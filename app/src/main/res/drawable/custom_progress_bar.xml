<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 1. Lớp nền (Background) của thanh tiến độ - Màu xám nhạt -->
    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="10dp" />
            <solid android:color="#E0E4EC" />
        </shape>
    </item>

    <!-- 2. Lớp hiển thị tiến độ (Progress) - Màu xanh chủ đạo của App -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="10dp" />
                <gradient
                    android:startColor="#4A6CF7"
                    android:endColor="#6C86F7"
                    android:angle="0" />
            </shape>
        </clip>
    </item>

</layer-list>